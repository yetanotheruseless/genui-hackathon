/**
 * @genui/shared-state — @colyseus/schema classes shared between the
 * server Room and the cockpit iframe. Pure schema definitions, no
 * runtime logic, no game-specific imports.
 */
export { Player } from "./player.js";
export { World } from "./world.js";
